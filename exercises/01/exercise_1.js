const { stringToSlug } = require("../../utils");

/**
 * TASK:
 * Use forEach() and stringToSlug() to create an array of cleaned categories
 *
 * stringToSlug() takes a string and returns a clean version of the string
 * Example: Übersicht => uebersicht
 */
const toSlug = (categories) => {
  // Your code goes here
};

module.exports = toSlug;

/*
 * https://developer.mozilla.org/de/docs/Web/JavaScript/Reference/Global_Objects/Array/forEach
 */
