const { stringToSlug } = require("../../utils");

/**
 * 1. TASK:
 * Use map() to create an array of clean categories
 */
const toSlug = (categories) => {
  // Your code goes here:
};

/**
 * 2. TASK:
 * Use map() and a JS-Native function to get the result.
 * Why looks the result how it looks?
 *
 * INFO: This exercise shows a possibility of an error while using map()
 *       and needs to be thinked "um die Ecke gedacht"
 *
 * HINT: https://lodash.com/docs/4.17.15#unary
 */
const toInt = (arrayOfStrings) => {
  // Your code goes here:
};

/**
 * 3. TASK:
 * Project an array of advices into an array of { id, title } pairs
 */
const toAdviceTuple = (advices) => {
  // Your code goes here:
};

module.exports = {
  toSlug,
  toInt,
  toAdviceTuple,
};

/*
 * https://developer.mozilla.org/de/docs/Web/JavaScript/Reference/Global_Objects/Array/map
 */
